# TrainingPeaks auth cookie — copy from browser DevTools → Application → Cookies
# Format: Production_tpAuth=<value>
TP_AUTH_COOKIE=Production_tpAuth=YOUR_COOKIE_VALUE_HERE

# Number of days to shift plan start when applying temporarily (default 0)
TP_PLAN_DAYS_SHIFT=0
